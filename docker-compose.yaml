version: '3'
services:
  postgres:
    container_name: postgres
    image: postgres
    environment:
      - HOSTNAME=postgres
      - POSTGRES_PASSWORD=docker
    ports:
      - 5432:5432
    volumes:
      - db-data:/var/lib/postgresql/data
    restart: always

volumes:
  db-data:
    driver: local